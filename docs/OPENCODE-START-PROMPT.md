# OpenCode starting prompt

Continue Dotneet from the attached source archive in a Linux environment. Preserve the existing Next.js frontend, Node.js route handlers, PostgreSQL/Prisma data model and verified work.

Read README.md and docs/VALIDATION.md first. Then read the PRD, FRD, architecture, data/API specification, security requirements, implementation/traceability plan, Linux operations guide and design system. Read AGENTS.md and the installed Next.js documentation relevant to your changes.

Dotneet’s promise is “Get paid. Keep the proof.” The MVP is a wallet-bound .neet identity, NIM contribution payment, separately signed acknowledgment, independently checked receipt and recipient-controlled publication. The first audience is Nimiq builders and contributors. Maintain a solo-developer eight-day scope; no marketplace, escrow, multi-chain platform, token incentive system or universal reputation score.

Inspect the current source and manifest before changing it. Reproduce the local preview and the listed checks using a fresh development database. Continue from the earliest unmet release requirement in VALIDATION.md. Native Nimiq Pay and real indexed-RPC behavior require actual evidence; mocked tests do not close those gates.

Preserve same-origin session protection, one-use ownership challenges, exact signing bytes, address derivation, receipt ownership, integer Luna amounts, payment-network matching, attempt tokens, independent transaction checks, safe reconciliation, acknowledgment immutability and publication consent. Never turn a sample or test fixture into a production success bypass. Keep legacy credibility records isolated and legacy names protected for their original wallet.

For each bounded change, report the requirement IDs addressed, files changed, why the behavior changed, checks actually run and remaining blockers. Keep implementation and documentation synchronized. Use shared design tokens/components and update cross-format exports when foundations change.

If code and documents disagree, inspect the actual contract and validation evidence, record the discrepancy and make a deliberate correction. Resolve routine reversible implementation choices yourself. Ask only when missing credentials, device access, authorization or a material product decision blocks the next step; continue unaffected work.

The definition of done is the agreed MVP with explicit evidence for its supported environment, accurate status and a complete handoff. No public deployment, public communication, remote push or real payment is authorized by this continuation prompt alone. Provide concrete instructions for a human/device test where access is unavailable.
